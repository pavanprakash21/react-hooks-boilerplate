import React from "react";

const HookTimer = () => {
  return (
    <>
      <h2>Hook Timer - {/* code here */}</h2>
      <button
        type="button"
        onClick={/* code here */}
      >
        Stop Running
      </button>
    </>
  );
};

export default HookTimer;
