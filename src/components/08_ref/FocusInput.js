import React from "react";

// TODO: On page load, make it so that the input takes focus
const FocusInput = () => {
  return (
    <div>
      <input type="text" />
    </div>
  );
};

export default FocusInput;
